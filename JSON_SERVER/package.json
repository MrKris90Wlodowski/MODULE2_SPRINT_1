{
  "name": "json_server",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "start": "json-server --watch db.json --host localhost --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
